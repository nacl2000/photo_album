load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/nacl2000/photo_album
# bazel run //:gazelle -- update-repos -from_file=go.mod -to_macro=go_dependencies.bzl%go_dependencies
gazelle(name = "gazelle")
